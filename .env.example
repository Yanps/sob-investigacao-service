# ===========================================
# Sob Investigação Service - Variáveis de ambiente
# Copie para .env e preencha os valores.
# ===========================================

# --- Google Cloud / Firebase ---
# Caminho para o JSON da conta de serviço (desenvolvimento local).
# Em produção (Cloud Run, etc.) use Application Default Credentials.
GOOGLE_APPLICATION_CREDENTIALS=/caminho/para/sua-conta-servico.json

# Projeto GCP (opcional; pode ser inferido pela credencial).
# GCP_PROJECT=meu-projeto-gcp

# --- Servidor ---
# Porta do servidor HTTP (webhook) e do worker.
# PORT=8080

# --- WhatsApp (Meta Cloud API) ---
# URL base da API do WhatsApp (ex.: https://graph.facebook.com/v18.0).
WHATSAPP_API_URL=
# Token de acesso do app Meta.
WHATSAPP_ACCESS_TOKEN=
# ID do número de telefone do WhatsApp Business.
WHATSAPP_PHONE_NUMBER_ID=
# Token usado na verificação do webhook (GET) pelo Meta.
WHATSAPP_WEBHOOK_VERIFY_TOKEN=

# --- Vertex AI (agente) ---
# Número do projeto GCP (apenas dígitos).
VERTEX_AI_PROJECT_NUMBER=
# Região (ex.: us-central1).
VERTEX_AI_LOCATION=
# ID do Reasoning Engine / agente no Vertex AI.
VERTEX_AI_AGENT_ENGINE_ID=

# --- Pub/Sub ---
# O tópico "sob-processing-jobs" deve existir no projeto.
# Credenciais: mesmo GOOGLE_APPLICATION_CREDENTIALS ou ADC.

# --- Segurança (opcional) ---
# Token para chamadas internas ao webhook (middleware auth).
# INTERNAL_TOKEN=
